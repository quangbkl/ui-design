import React from 'react';
import {Header as HeaderReact} from 'react-native-elements';

const LeftComponent = () => {

};

const Header = () => {
    return (
        <HeaderReact/>
    );
};
}

export default Header;
