import React from 'react';
import {Header as HeaderReact} from 'react-native-elements';

const LeftComponent = () => {

};

class Header extends React.Component {
    render() {
        return (
            <HeaderReact/>
        );
    }
}

export default Header;
