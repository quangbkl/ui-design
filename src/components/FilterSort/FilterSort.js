import React from 'react';
import {Text, View} from 'react-native';

const FilterSort = () => {
    return (
        <View>
            <Text>FilterSort</Text>
        </View>
    );
};

export default FilterSort;
