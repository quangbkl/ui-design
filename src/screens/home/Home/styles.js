import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: '#29ab54',
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});
